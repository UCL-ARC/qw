on:
  issues:
    types: [opened, reopened, edited]


env:
  # Adding in token so we can use gh CLI
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  issue-parsing:
    runs-on: ubuntu-latest
    steps:
      - name: parse from event
        run: |
          echo ${TITLE} \
          && echo ${BODY} \
          && echo ${NUMBER} \
        env:
          TITLE: ${{ github.event.issue.title }}
          BODY: ${{ github.event.issue.body }}
          NUMBER: ${{ github.event.issue.number }}
      - name: assignees
        run: |
          gh issue view ${{ github.event.issue.html_url }} \
                      --json "assignees" --jq '.assignees | map(.login) | join(", ")'
      - name: label check
        if: ${{ github.event.issue.label.name == 'enhancement' }}
        run : |
          echo "enhancement label found"